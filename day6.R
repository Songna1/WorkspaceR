#
#6일차
#
# Topic
#  1 다중변수 자료 탐색
#  2 상관분석
#  3 자료탐색 실습( EDA )
#

#1. 다중변수 자료 탐색
#
#-다중변수 (다변량) 자료는 변수가 2개 이상인 자료
#-다중변수 자료는 2차원 형태, matrix나 data frame에 저장하여 분석
#-다중변수 자료에서 변수는 열(column),
# 개별 관측값들이 행(row)로 구성
#
#산점도(scatter plot) 2개의 변수로 구성된 자료의 분포를 알아보는 그래프
#                    관측값들의 분포를 ★2개의 변수 사이의 관계★를 파악할 수 있는 기법
#(ex.키와 몸무게 관계, 공부시간과 점수의 관계)
#- 산점도는 두 변수의 데이터 분포를 나타내는 것이기 때문에 두개의 변수에 대한
#  자료가 필요
#- 산점도는 관측값들의 분포를 보면서 두 변수 사이의 관련성을 확인하는데 사용
#